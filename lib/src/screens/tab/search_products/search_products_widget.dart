import 'package:flutter/material.dart';

class SearchProductsWidget extends StatefulWidget{

  static const String routeName = "/searchProduct";

  const SearchProductsWidget({Key? key}) : super(key: key);

  @override
  SearchProductWidgetState createState() {
   return SearchProductWidgetState();
  }
}

class SearchProductWidgetState extends State<SearchProductsWidget>{
  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}