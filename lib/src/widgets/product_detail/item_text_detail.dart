import 'package:flutter/cupertino.dart';

class ItemTextDetail extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Container(
          child: Text(
            ""
          ),
        )
      ],
    );
  }
}