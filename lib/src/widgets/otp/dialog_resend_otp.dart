import 'package:flutter/material.dart';

class DialogResendOtp{

  static void showDialogResendOtp(BuildContext context){
    showDialog(context: context, builder: (_) {
      return Container(
        child: Column(
          children: [

          ],
        ),
      );
    });
  }
}